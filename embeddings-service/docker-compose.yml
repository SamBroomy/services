services:
  embeddings:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: embeddings-service
    image: embeddings_service
    #restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
